import { LockedPage } from "./LockedPage";

export function Channels() {
  return (
    <LockedPage
      title="Channels"
      sentence={
        "Where a phone number gets attached to an agent. ShipVoice Pro does ship the parts under " +
        "this screen: the SIP and telephony wiring, plus a written 10DLC registration runbook you " +
        "follow to get a number approved. The provisioning screen itself is only designed, not " +
        "built, in Pro either, so paying does not hand you this page."
      }
    />
  );
}
