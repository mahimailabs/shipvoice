import { LockedPage } from "./LockedPage";

export function Evaluations() {
  return (
    <LockedPage
      title="Evaluations"
      sentence={
        "Scoring an agent against messy scripted calls before you let it take a real one: the " +
        "caller who interrupts, the lookup that comes back empty, the person who says now is a bad " +
        "time. Designed in ShipVoice Pro, not built there yet, so treat it as the roadmap rather " +
        "than a reason to buy."
      }
    />
  );
}
