import { LockedPage } from "./LockedPage";

export function Campaigns() {
  return (
    <LockedPage
      title="Campaigns"
      sentence={
        "Running an outbound campaign: a lead list, the pace it dials at, and the calling window " +
        "it has to stay inside. The screen is designed in ShipVoice Pro, but the runner behind it " +
        "is not built there yet, so this is a look at where the product is going, not something " +
        "paying for Pro switches on today."
      }
    />
  );
}
